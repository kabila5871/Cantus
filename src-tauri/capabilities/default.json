{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and any folder windows (win-*)",
  "windows": ["main", "win-*"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
